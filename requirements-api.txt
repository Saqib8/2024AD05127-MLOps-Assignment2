# Inference only. Deliberately smaller than requirements.txt so the container
# stays light: no mlflow, no matplotlib, no training extras.
# torch and torchvision come from the CPU index (see Dockerfile), the CUDA
# build would add well over a gigabyte for no benefit when serving.

torch==2.13.0
torchvision==0.28.0
numpy==2.4.6
pillow==11.3.0
fastapi==0.139.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic==2.13.4
prometheus-client==0.25.0
